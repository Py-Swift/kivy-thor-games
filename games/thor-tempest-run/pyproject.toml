[project]
name = "thor-tempest-run"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "psychowasp", email = "psychowaspx@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pymunk>=7.2.0",
    "thorvg-cython",
    "kivy",
]

[project.scripts]
thor-tempest-run = "thor_tempest_run:main"

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"

[tool.uv]
find-links = ["../wheelhouse"]
